spring:
  profiles:
    active: development
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
        default_batch_fetch_size: 100
    open-in-view: false
  datasource:
    url: jdbc:mysql://dev-barista-cluster.cluster-cejbmksulyv2.ap-northeast-2.rds.amazonaws.com:3306/collectcard?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: collectcard_was
    password: ${DEVELOPMENT_COLLECTCARD_DB_PASSWORD}
    hikari:
      idle-timeout: 300000
      minimum-idle: 10
      maximum-pool-size: 30
      connection-timeout: 5000
      validation-timeout: 4000
      max-lifetime: 1800000

logging:
  level:
    com.zaxxer.hikari.HikariConfig: DEBUG
    com.zaxxer.hikari: TRACE
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

host:
  card:
    shinhancard: https://devopenapi.shinhancard.com:8443

shinhancard:
  clientId: ${DEVELOPMENT_SHINHANCARD_CLIENT_ID}
  organizationObjectid: 596d66692c4069c168b57c59
